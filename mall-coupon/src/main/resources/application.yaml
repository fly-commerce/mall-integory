spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.1.204:3306/yqz-fly-commerce-marketing
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

# 优先读取bootstrap.yaml中配置中心的配置
coupon:
  user:
    name: zhangsan
    age: 12
